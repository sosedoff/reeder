<!DOCTYPE html>
<html lang="en" ng-app="reeder">
  <head>
    <meta charset="utf-8">
    <title>Reeder</title>
    <%= css :app, :media => 'screen' %>
  </head>
  <body>
    <div id="wrapper">
      <div class="inner">
        <%= render_partial 'partials/_header' %>

        <main role="main" id="main" ng-controller="ReederSignupController">
          <div class="onBoard signup">
            <div class="formTitle">
              <h1>Sign Up</h1>
              <p>Be nice, eat your veggies and fill out some info.</p>
            </div>

            <form novalidate class="simple-form">
              <div>
                <i class="icon-envelope-alt"></i>
                <input type="text" ng-model="user.email" placeholder="email" />
              </div>
              <div>
                <i class="icon-smile"></i>
                <input type="text" ng-model="user.name"  placeholder="name" />
              </div>
              <div>
                <i class="icon-lock"></i>
                <input type="password" ng-model="user.password"  placeholder="password" />
              </div>
              <div>
                <i class="icon-lock"></i>
                <input type="password" ng-model="user.password_confirmation" placeholder="confirm password" />
              </div>

              <button ng-click="create_account()">Create Account</button>
            </form>
          </div>
        </main>
      </div>
    </div>

    <%= js :app %>
  </body>
</html>

